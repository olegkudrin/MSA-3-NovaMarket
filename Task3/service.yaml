apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  labels:
    app: scaletestapp
spec:
  type: NodePort
  selector:
    pod: scaletestapp-pod
  ports:
    - name: main-port
      port: 8080
      targetPort: 8080
      nodePort: 30080
